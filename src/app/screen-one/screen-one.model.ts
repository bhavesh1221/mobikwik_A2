//this interface denotes the type of the variable which we have used to create bank data
export interface Bank {
    id: Number,
    bankName: string;
    accountNumber: number;
    imgPath: string,
    status:boolean
}
